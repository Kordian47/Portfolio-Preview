Fully implement (do not just describe) a new “Engineering Artifacts” feature inside my existing React + TypeScript app. My project structure is client/src/ with pages/, components/, data/, and App.tsx, so all new files must be created inside those directories. Create the following files: client/src/pages/Artifacts.tsx, client/src/components/ArtifactModal.tsx, and client/src/data/artifacts.ts, and ensure they appear in the file tree. In client/src/App.tsx, import the Artifacts page and add a new route for /projects/artifacts without breaking existing routes. In client/src/pages/Projects.tsx, add a new section below the Featured Projects section titled “Engineering Artifacts” with a single card styled consistently with existing project cards, titled “Engineering Artifacts & Rapid Builds,” with the description “A collection of smaller builds, fixtures, jigs, and rapid mechanical solutions,” and make that card navigate to /projects/artifacts. In Artifacts.tsx, create a page with a title “Engineering Artifacts,” a short intro paragraph explaining this is a collection of smaller fabrication builds and rapid engineering solutions, and below that a responsive grid gallery (3 columns desktop, 2 tablet, 1 mobile) of artifact cards showing a thumbnail image, title, one-line description, and subtle hover animation. In client/src/data/artifacts.ts, export an array of at least three placeholder artifact objects containing id, title, shortDescription, images (string array), description, tools (string array), and takeaway. When clicking an artifact card, open a centered modal overlay (do not navigate to a new route) that dims the background, locks background scrolling, has a max height of 90vh, scrolls internally if content is tall, and closes when clicking an X button, clicking outside the modal, or pressing Escape. Implement this modal as a reusable component in client/src/components/ArtifactModal.tsx that receives artifact and onClose props, and in Artifacts.tsx manage state using const [selectedArtifact, setSelectedArtifact] = useState<Artifact | null>(null); rendering the modal conditionally when selectedArtifact is not null, ensuring Escape listeners are properly cleaned up. Keep styling consistent with the existing Tailwind theme, add subtle modal open/close animation (use Framer Motion if already installed), do not make the modal full screen, and ensure all files are saved and written to disk so that running git status shows the new and modified files.